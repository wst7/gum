import type { ReactNode } from 'react';
export interface ContainerProps {
    children: ReactNode;
    title?: string;
    border?: boolean;
    padding?: number;
    muted?: boolean;
    style?: Record<string, unknown>;
}
export declare function Container({ children, title, border, padding, muted, style }: ContainerProps): ReactNode;
//# sourceMappingURL=container.d.ts.map