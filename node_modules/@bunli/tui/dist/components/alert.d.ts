import type { ReactNode } from 'react';
import { type ComponentEmphasis, type ComponentSize, type ComponentTone } from './variant.js';
export type AlertTone = Extract<ComponentTone, 'info' | 'success' | 'warning' | 'danger'>;
export interface AlertProps {
    tone?: AlertTone;
    size?: ComponentSize;
    emphasis?: ComponentEmphasis;
    title?: string;
    message: string;
    children?: ReactNode;
    fg?: string;
    bg?: string;
    borderColor?: string;
    padding?: number;
}
export declare function Alert({ tone, size, emphasis, title, message, children, fg, bg, borderColor, padding }: AlertProps): ReactNode;
//# sourceMappingURL=alert.d.ts.map