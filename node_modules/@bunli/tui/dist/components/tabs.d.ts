import type { ReactNode } from 'react';
export interface TabItem {
    key: string;
    label: string;
    content: ReactNode;
}
export interface TabsProps {
    tabs: TabItem[];
    initialKey?: string;
    activeKey?: string;
    onChange?: (key: string) => void;
    scopeId?: string;
    keyboardEnabled?: boolean;
}
export declare function Tabs({ tabs, initialKey, activeKey, onChange, scopeId, keyboardEnabled }: TabsProps): ReactNode;
//# sourceMappingURL=tabs.d.ts.map