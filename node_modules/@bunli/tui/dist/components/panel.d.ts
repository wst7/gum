import type { ReactNode } from 'react';
import { type ComponentEmphasis, type ComponentSize, type ComponentTone } from './variant.js';
export type PanelTone = Extract<ComponentTone, 'default' | 'accent' | 'success' | 'warning' | 'danger'>;
export interface PanelProps {
    title?: string;
    subtitle?: string;
    footer?: ReactNode;
    tone?: PanelTone;
    size?: ComponentSize;
    emphasis?: ComponentEmphasis;
    padded?: boolean;
    children?: ReactNode;
    fg?: string;
    bg?: string;
    borderColor?: string;
}
export declare function Panel({ title, subtitle, footer, tone, size, emphasis, padded, children, fg, bg, borderColor }: PanelProps): ReactNode;
//# sourceMappingURL=panel.d.ts.map