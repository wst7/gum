import type { ReactNode, Ref } from 'react';
import type { ScrollBoxRenderable } from '@opentui/core';
import { type ComponentEmphasis, type ComponentSize } from './variant.js';
import type { PanelTone } from './panel.js';
export interface ScrollPanelScrollbarOptions {
    visible?: boolean;
    trackColor?: string;
    thumbColor?: string;
}
export interface ScrollPanelProps {
    id?: string;
    title?: string;
    subtitle?: string;
    chromeLineWidth?: number;
    header?: ReactNode;
    footer?: ReactNode;
    tone?: PanelTone;
    size?: ComponentSize;
    emphasis?: ComponentEmphasis;
    padded?: boolean;
    focused?: boolean;
    stickyHeader?: boolean;
    stickyFooter?: boolean;
    height?: number | `${number}%` | 'auto';
    width?: number | `${number}%` | 'auto';
    fg?: string;
    bg?: string;
    borderColor?: string;
    bodyGap?: number;
    bodyPadding?: number;
    allowHorizontalScroll?: boolean;
    bodyRef?: Ref<ScrollBoxRenderable>;
    scrollbar?: ScrollPanelScrollbarOptions;
    children?: ReactNode;
}
export declare function ScrollPanel({ id, title, subtitle, chromeLineWidth, header, footer, tone, size, emphasis, padded, focused, stickyHeader, stickyFooter, height, width, fg, bg, borderColor, bodyGap, bodyPadding, allowHorizontalScroll, bodyRef, scrollbar, children }: ScrollPanelProps): ReactNode;
//# sourceMappingURL=scroll-panel.d.ts.map