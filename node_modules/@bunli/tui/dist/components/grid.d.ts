import type { ReactNode } from 'react';
export interface GridProps {
    children: ReactNode;
    columns?: number;
    gap?: number;
}
export declare function Grid({ children, columns, gap }: GridProps): ReactNode;
//# sourceMappingURL=grid.d.ts.map