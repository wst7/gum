import type { ReactNode } from 'react';
import { type PanelTone } from './panel.js';
import type { ComponentEmphasis, ComponentSize } from './variant.js';
export interface CardProps {
    title: string;
    description?: string;
    tone?: PanelTone;
    size?: ComponentSize;
    emphasis?: ComponentEmphasis;
    children?: ReactNode;
}
export declare function Card({ title, description, tone, size, emphasis, children }: CardProps): ReactNode;
//# sourceMappingURL=card.d.ts.map