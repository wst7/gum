import { type TextOverflowMode } from '@bunli/runtime/app';
export interface CommandPaletteItem {
    key: string;
    label: string;
    hint?: string;
}
export interface CommandPaletteProps {
    items: CommandPaletteItem[];
    placeholder?: string;
    onSelect?: (key: string) => void;
    scopeId?: string;
    keyboardEnabled?: boolean;
    inputFocused?: boolean;
    maxLineWidth?: number;
    overflow?: TextOverflowMode;
}
export declare function CommandPalette({ items, placeholder, onSelect, scopeId, keyboardEnabled, inputFocused, maxLineWidth, overflow }: CommandPaletteProps): import("react").ReactNode;
//# sourceMappingURL=command-palette.d.ts.map