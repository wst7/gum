/**
 * Help rendering module — extracted from cli.ts for reuse and testability.
 */
import type { Command, HelpRenderer, TerminalInfo } from "../types.js";
/**
 * Context for the built-in help renderer.
 */
export interface HelpContext {
    cliName: string;
    version: string;
    description?: string;
    /** Resolved terminal info for width calculations */
    terminal: TerminalInfo;
}
/**
 * Collect top-level commands from a command map (excluding alias duplicates and nested commands).
 */
export declare function collectTopLevelCommands<TStore = {}>(commands: Map<string, Command<any, TStore>>): Command<any, TStore>[];
/**
 * Render root help (no specific command).
 */
export declare function renderRootHelp<TStore = {}>(ctx: HelpContext, commands: Command<any, TStore>[]): string;
/**
 * Render help for a specific command.
 */
export declare function renderCommandHelp<TStore = {}>(ctx: HelpContext, cmd: Command<any, TStore>, path?: string[]): string;
/**
 * Unified help function — dispatches to custom renderer or built-in renderer.
 */
export declare function showHelp<TStore = {}>(ctx: HelpContext, commands: Map<string, Command<any, TStore>>, customRenderer: HelpRenderer<TStore> | undefined, cmd?: Command<any, TStore>, path?: string[]): void;
export declare function wrapText(text: string, width: number): string[];
export declare function formatTwoColumnRows(rows: Array<{
    label: string;
    description: string;
}>, terminalWidth: number): string[];
//# sourceMappingURL=renderer.d.ts.map