import type { Command } from "../types.js";
/**
 * Generates a compact markdown command index for `--llms`.
 */
export declare function renderIndex(cliName: string, commands: Map<string, Command<any, any>>, description?: string): string;
/**
 * Generates a full manifest for `--llms-full`.
 */
export declare function renderFull(cliName: string, commands: Map<string, Command<any, any>>, description?: string): string;
export { toJsonSchema, resolveTypeName } from "./schema.js";
//# sourceMappingURL=index.d.ts.map